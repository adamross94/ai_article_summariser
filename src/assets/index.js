/* Imports several SVG and PNG image files from different paths and exports them as a 
collection of variables. These variables can then be used in other parts of the codebase 
to easily reference and display these images. */

import linkIcon from './link.svg'
import loader from './loader.svg'
import copy from './copy.svg'
import tick from './tick.svg'
import logoTest from './logoTest.png'

export {
    linkIcon,
    loader,
    copy,
    tick,
    logoTest
}